{
  "tasks": {
    "compile": "deno compile --allow-read --allow-run --allow-env --allow-net -o vvproxy.exe main.ts",
    "watch": "deno run --allow-read --allow-run --allow-env --allow-net --watch main.ts"
  }
}
